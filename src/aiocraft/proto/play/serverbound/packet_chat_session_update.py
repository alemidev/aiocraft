"""[!] This file is autogenerated"""

from typing import Tuple, List, Dict, Union, Optional
from ....packet import Packet
from ....types import *
from ....primitives import *

class PacketChatSessionUpdate(Packet):
	__slots__ = ( 'id', 'expireTime', 'publicKey', 'sessionUUID', 'signature' )
	
	expireTime : int
	publicKey : bytes
	sessionUUID : str
	signature : bytes

	def __init__(self, 
		expireTime:int | None = None,
		publicKey:bytes | None = None,
		sessionUUID:str | None = None,
		signature:bytes | None = None,
		**kwargs
	):
		super().__init__(
			expireTime=expireTime,
			publicKey=publicKey,
			sessionUUID=sessionUUID,
			signature=signature
		)

	_state : int = 3

	_ids : Dict[int, int] = {
		761 : 32
	}
	_definitions : Dict[int, List[Tuple[str, Type]]] = {
		761 : [ ( 'sessionUUID', UUID ), ( 'expireTime', Long ), ( 'publicKey', ByteArray ), ( 'signature', ByteArray ) ]
	}
